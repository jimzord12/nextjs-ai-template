# Process Recording Diary

## Header

Project Name: nextjs-ai-template
Repository / Link: local
Author(s): jimzord-dev
Start Date: 2026-05-31
Tech Stack: Next.js, TypeScript, Biome, pnpm, Vitest
Methodology: Kanban

---

## Daily / Session Entry

Date: 2026-05-31
Duration: ~3h
Author: jimzord-dev

### Focus of the Day

Initial project setup — scaffold Next.js AI template repo, configure tooling, and make the first commit.

### What Was Done

- Initialized Next.js project with TypeScript, Biome (lint + format), commitlint, Husky, lint-staged, Vitest
- Added shadcn/ui component library with initial components (button, card, dialog, input, etc.)
- Set up project architecture: `src/app`, `src/components`, `src/features`, `src/shared`, `src/lib`
- Added agent skills (20+ skills) via `.agents/` directory including vercel-optimize, tdd, triage, diagnose, and others
- Added project documentation: ARCHITECTURE.md, CONVENTIONS.md, ROUTINES.md, WORKING_WITH_ME.md, RULES.md
- Set up CI via `.github/workflows/ci.yml`
- Added "No Solo Decisions" rule to WORKING_WITH_ME.md after agent made unsolicited `.gitignore` change
- Configured biome.json override for `.agents/**` to relax rules on installed skill files (formatter off, specific lint rules off)

### Thinking & Rationale

- The biome override approach was chosen over auto-fixing or inline suppressions because `.agents/` contains third-party installed skill files (339 files) that shouldn't be structurally modified — they're managed by the skills system
- Agent skills are committed to the repo (not gitignored) because this is an AI development template meant to be cloned with all tooling intact

### Blockers & Issues

- **Biome vs installed skills**: `.agents/` skill files (especially vercel-optimize) triggered ~50 biome errors (quote style, import order, regex patterns, unused params). Resolved by adding a biome override that relaxes specific rules for `.agents/**` only.
- **Stale .next cache**: TypeScript errors from `.next/dev/types/validator.ts` referencing pages that don't exist. Fixed by clearing the `.next` directory.
- **Unsolicited config change**: Agent added `.agents/` to `.gitignore` without asking — this was wrong for a template repo. Reverted and added "No Solo Decisions" rule to WORKING_WITH_ME.md.
- Status: all resolved

### Insights & Learnings

- Biome v2 doesn't have a `files.ignore` key — overrides with `includes` and rule-level overrides are the correct approach
- When biome.json has a parse error, `biome check --write` runs with default settings (tabs) which can corrupt indentation across the project — always verify config validity before running `--write`

### References & Links

- Initial commit: `99bc87e`
- 4 commits total on `main` branch

### Next Steps

- Build out the marketing pages (about, contact, faq, rooms)
- Set up feature module architecture pattern
- Add database/authentication layer
- Configure CI pipeline to run full check suite
